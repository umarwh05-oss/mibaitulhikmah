"use client";
import {useState} from "react";
export default function PPDB(){const[msg,setMsg]=useState("");const[loading,setLoading]=useState(false);
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMsg("");const r=await fetch("/api/ppdb",{method:"POST",body:new FormData(e.currentTarget)});const j=await r.json();setLoading(false);if(r.ok){setMsg("Pendaftaran berhasil. Nomor pendaftaran: "+j.registration_number);e.currentTarget.reset()}else setMsg(j.error||"Pendaftaran gagal")}
return <section><div className="wrap"><h1 style={{fontSize:42}}>PPDB Online</h1><p className="muted">MIS Baitul Hikmah — Kabupaten Tebo</p>{msg&&<div className="notice">{msg}</div>}<form className="card form" onSubmit={submit}><div className="two"><div className="field"><label>Nama lengkap *</label><input name="nama_lengkap" required/></div><div className="field"><label>NIK *</label><input name="nik" inputMode="numeric" required/></div></div><div className="two"><div className="field"><label>Jenis kelamin</label><select name="jenis_kelamin"><option value="L">Laki-laki</option><option value="P">Perempuan</option></select></div><div className="field"><label>Tempat, tanggal lahir</label><input name="ttl"/></div></div><div className="two"><div className="field"><label>Nama orang tua/wali *</label><input name="nama_wali" required/></div><div className="field"><label>No. WhatsApp *</label><input name="no_wa" required/></div></div><div className="field"><label>Asal TK/RA/PAUD</label><input name="asal_sekolah"/></div><div className="field"><label>Alamat</label><textarea name="alamat" rows={4}/></div><div className="two"><div className="field"><label>KK</label><input name="kk" type="file" accept=".pdf,.jpg,.jpeg,.png"/></div><div className="field"><label>Akta kelahiran</label><input name="akta" type="file" accept=".pdf,.jpg,.jpeg,.png"/></div></div><button className="btn" disabled={loading}>{loading?"Mengirim...":"Kirim Pendaftaran"}</button></form></div></section>}