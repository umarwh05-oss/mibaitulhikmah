import {supabaseServer} from "@/lib/supabase-server";
import {notFound} from "next/navigation";
export default async function Detail({params}:{params:Promise<{registration_number:string}>}){const p=await params;const sb=await supabaseServer();const{data}=await sb.from("ppdb_applicants").select("*").eq("registration_number",p.registration_number).single();if(!data)notFound();return <section><div className="wrap"><h1 style={{fontSize:38}}>Detail PPDB</h1><div className="card"><p><b>Nomor:</b> {data.registration_number}</p><p><b>Nama:</b> {data.nama_lengkap}</p><p><b>NIK:</b> {data.nik}</p><p><b>Wali:</b> {data.nama_wali}</p><p><b>WhatsApp:</b> {data.no_wa}</p><p><b>Asal sekolah:</b> {data.asal_sekolah||"-"}</p><p><b>Alamat:</b> {data.alamat||"-"}</p><p><b>Status:</b> <span className="pill">{data.status}</span></p><p className="muted">Untuk mengubah status, endpoint admin dapat dikembangkan setelah role admin ditetapkan.</p></div></div></section>}