import {supabaseServer} from "@/lib/supabase-server";
import Link from "next/link";
export default async function Admin(){const sb=await supabaseServer();const{data:{user}}=await sb.auth.getUser();const{data:rows}=await sb.from("ppdb_applicants").select("registration_number,nama_lengkap,no_wa,status,created_at").order("created_at",{ascending:false}).limit(20);return <main><div className="adminbar"><div className="wrap"><b>ADMIN MIS BAITUL HIKMAH</b><span style={{float:"right"}}>{user?.email}</span></div></div><section><div className="wrap"><h1 style={{fontSize:40}}>Dashboard PPDB</h1><p className="muted">Pendaftar terbaru dan status verifikasi.</p><div className="card"><table className="table"><thead><tr><th>No. PPDB</th><th>Nama</th><th>WhatsApp</th><th>Status</th><th>Aksi</th></tr></thead><tbody>{(rows||[]).map((x:any)=><tr key={x.registration_number}><td>{x.registration_number}</td><td>{x.nama_lengkap}</td><td>{x.no_wa}</td><td><span className="pill">{x.status}</span></td><td><Link href={"/admin/ppdb/"+x.registration_number}>Detail</Link></td></tr>)}</tbody></table></div></div></section></main>}